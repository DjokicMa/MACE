# Pinned test dependencies so the regression gate isn't at the mercy of upstream
# major releases (a breaking pytest/numpy major could otherwise turn CI red with
# no change in this repo). Loosen deliberately when intentionally upgrading.
pytest<9
pyyaml
numpy<3
# Core runtime features under test: the terminal UI layer is rich-based and the
# spectra/plot renderers are matplotlib-based. plotly/ase/spglib/scipy stay
# optional — tests needing them skip via pytest.importorskip.
rich<15
matplotlib<4
