[pytest]
# Regression suite lives in tests/. The interactive menu tests under Crystal_d12/
# are run separately (they monkeypatch stdin), so keep discovery scoped here.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
