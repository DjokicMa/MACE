# MACE - Mendoza Automated CRYSTAL Engine
# Core dependencies for MACE functionality

# Scientific computing
numpy>=1.21.0
matplotlib>=3.5.0

# Atomic simulation and structure handling
ase>=3.22.0

# Symmetry analysis
spglib>=1.16.0

# Themed terminal UI (optional but recommended: mace degrades to plain
# text without it; NO_COLOR / TERM=dumb are honored either way)
rich>=13.0

# Configuration and data handling
pyyaml>=6.0
pandas>=1.3.0

# Interactive plotting engines (mace plotting --cube / --freq)
# crystal_cubeviz_plotly + vibmode_viewer build plotly figures; scipy provides
# RegularGridInterpolator for non-orthogonal cube grids. kaleido is pinned to
# 0.2.1 (the stable pre-v1 release) for static PNG/SVG/PDF export; HTML output
# needs only plotly. Tested with plotly 6.0.1 / scipy 1.15.2 / kaleido 0.2.1.
plotly>=5.0.0
scipy>=1.10.0
kaleido==0.2.1

# Optional but recommended: Accurate band structure k-path generation
# Implements HPKOT methodology (Hinuma et al., Comp. Mat. Sci. 128, 140 (2017))
# Uncomment the line below to include seekpath
# seekpath
